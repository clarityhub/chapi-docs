import React from 'react';

import Typography from '@clarityhub/unity-web/lib/components/Typography';

const ArticleLead = ({ children }) => (
	<Typography type="p">{children}</Typography>
);

export default ArticleLead;
